<!DOCTYPE html>
<html>
  <head>
    <title>AuthWithQrcode</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_include_tag 'https://rawgit.com/schmich/instascan-builds/master/instascan.min.js' %>
    
  </head>

  <body>
    <% flash.each do |key, value| %>
    <div class="flash <%= key %>"><%= value %></div>
  <% end %>
    <%= yield %>
  </body>
</html>
